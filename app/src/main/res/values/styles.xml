<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/montserrat</item>
        <item name="android:textColor">@color/primaryText</item>
    </style>

    <style name="AlertBanner" parent="@style/AppTheme">
        <item name="colorPrimary">@color/tangerine</item>
        <item name="android:background">?selectableItemBackground</item>
        <item name="android:drawableStart">@drawable/ic_info</item>
        <item name="android:drawableEnd">@drawable/ic_keyboard_arrow_right</item>
        <item name="android:drawablePadding">@dimen/alert_banner_drawable_padding</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textSize">@dimen/alert_banner_text_size</item>
        <item name="android:padding">@dimen/alert_banner_padding</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
        <item name="fontFamily">@font/montserrat</item>
    </style>
    
    <style name="BigButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/big_button_height</item>
    </style>

    <style name="BigButton.Blue" parent="BigButton">
        <item name="backgroundTint">@color/bluejay</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
